server:
  port: 2238
spring:
  application:
    name: LaptopRecommenderApplication
  datasource:
    url: jdbc:mysql://localhost:3306/Laptops
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
      show-sql: true